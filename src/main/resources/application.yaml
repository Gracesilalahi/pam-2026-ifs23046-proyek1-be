ktor:
  application:
    modules:
      - org.delcom.ApplicationKt.module
  deployment:
    # Ini kuncinya! Pakai port dari server, kalau gak ada baru pakai 8080
    port: ${PORT:8080}
    # WAJIB 0.0.0.0 agar bisa diakses oleh Nginx dan Android kamu nanti
    host: 0.0.0.0

  database:
    host: ${DB_HOST}
    port: ${DB_PORT}
    name: ${DB_NAME}
    user: ${DB_USER}
    password: ${DB_PASSWORD}

  jwt:
    secret: "${JWT_SECRET:rahasia_grace_123_abc}"
    issuer: "${JWT_ISSUER:https://pam-2026-ifs23046-proyek1-be.s1if.life/}"
    audience: "${JWT_AUDIENCE:https://pam-2026-ifs23046-proyek1-be.s1if.life/}"
    realm: "Access to the wardrobe"