ktor:
  application:
    modules:
      - org.delcom.ApplicationKt.module
  deployment:
    port: 8080
    host: 0.0.0.0

  database:
    host: ${DB_HOST}
    port: ${DB_PORT}
    name: ${DB_NAME}
    user: ${DB_USER}
    password: ${DB_PASSWORD}

  jwt:
    # Masukkan teksnya langsung, jangan pakai ${} dulu biar pasti jalan
    secret: "rahasia_grace_123_abc"
    issuer: "http://0.0.0.0:8080/"
    audience: "http://0.0.0.0:8080/"
    realm: "Access to the wardrobe"